<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baseconfig.pillar">

    <library
        android:name="${libName}"
        android:label="${libName}">

    </library>

</manifest>
